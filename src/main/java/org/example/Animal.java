package org.example;

// Declare an "Interface" type
// This interface has one method "sound()", so any class that implements it
// must implement (override) the sound method.

public interface Animal {
    public void sound();    // method signature (header) only - no implementation.
}
